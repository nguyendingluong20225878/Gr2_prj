{
  "extends": "../../tsconfig.json", // Kế thừa từ file gốc
  "compilerOptions": {
    "outDir": "dist",
    "rootDir": "src",
    
    // QUAN TRỌNG: Không được có dòng "typeRoots" ở đây!
    // TypeScript sẽ tự leo lên root để tìm types.
    
    // Nếu vẫn lỗi process, bỏ comment dòng dưới:
    // "types": ["node"]
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "dist", "**/*.test.ts"],
  "references": [
    { "path": "../shared" }
  ]
}